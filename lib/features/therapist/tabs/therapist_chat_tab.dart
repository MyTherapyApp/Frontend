import 'package:flutter/material.dart';

class TherapistChatTab extends StatelessWidget {
  const TherapistChatTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Therapist Chat")),
    );
  }
}
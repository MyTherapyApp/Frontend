import 'package:flutter/material.dart';

class TherapistProfileTab extends StatelessWidget {
  const TherapistProfileTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Therapist Profile")),
    );
  }
}